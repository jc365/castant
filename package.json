{
  "name": "castant",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest --run",
    "test:verbo": "vitest --run --reporter=verbose",
    "test:watch": "vitest",
    "test:coverage": "vitest --coverage --run",
    "test:integration": "vitest --run tests/integration",
    "test:back": "vitest --run",
    "test:backend": "vitest --run",
    "test:front": "cd frontend && npm test",
    "test:orch": "cd orchestration && PYTHONPATH=.. venv/bin/python -m pytest tests/ -v",
    "test:all": "bash scripts/run-all-tests.sh",

    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:migrate": "cd backend && npm run db:push",
    "db:studio": "cd backend && npx prisma studio",
    "db:backup": "cd backend && npm run db:backup",
    "db:restore": "cd backend && npm run db:restore",
    
    "dev:all": "concurrently --names \"DB,BACKEND,FRONTEND,OPENCODE,ORCHES\" --prefix-colors \"yellow,blue,green,magenta,cyan\" \"docker compose up\" \"cd backend && npm run dev\" \"cd frontend && npm run dev\" \"opencode web --port 3001\" \"cd orchestration && PYTHONPATH=.. venv/bin/python -m uvicorn orchestration.webhooks.server:app --host 0.0.0.0 --port 8080 --reload\"",

    "dev:orches": "cd orchestration && PYTHONPATH=.. venv/bin/python -m uvicorn orchestration.webhooks.server:app --host 0.0.0.0 --port 8080 --reload"
  },
  "devDependencies": {
    "@types/supertest": "^7.2.0",
    "@vitest/coverage-v8": "^4.1.10",
    "supertest": "^7.2.2",
    "vitest": "^4.1.10"
  }
}
